---
permalink: search.json
eleventyExcludeFromCollections: true
---
{"search" : [
{%- for item in collections.all -%}
  {
    "url" : "{{ item.url }}",
    "title" : "{{ item.data.title }}"{# "text" : "{{ (item.data.title + " " + item.templateContent) }}" #}
  }{% if not loop.last %},{% else %}{%- endif -%}
{%- endfor -%}
]}